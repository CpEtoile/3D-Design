#Convert binary into humain readable format for 3D stl files

## When export 3D design from web site https://www.tinkercad.com/ it is exported in binary format, to convert it into human readable format you can use the following python code:

```python src/binary_convertion.py```